<UserControl x:Class="HS.Wpf.ARO.Views.OperationRoomViews.CollectionActionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HS.Wpf.ARO.Views.OperationRoomViews"
             xmlns:converters="clr-namespace:HS.Wpf.Shared.Converters;assembly=HS.Wpf.Shared"
             xmlns:dt="clr-namespace:HS.Wpf.ARO.DesingTime"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=dt:OperationRoomCollectionActionsViewModelDesingTime, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HS.Wpf.Shared;component/Theme/Generics.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding Actions}" 
                  AutoGenerateColumns="False" 
                  SelectedItem="{Binding SelectedAction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeRows="False"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum úkonu" Binding="{Binding Model.IssueDate, StringFormat=d}" />
                <DataGridTextColumn Header="Popis" Binding="{Binding Model.Description}" />
                <DataGridTextColumn Header="Q1" Width="1*" Binding="{Binding Model.Q1, Converter={StaticResource BoolToStringConverter}}" />
                <DataGridTextColumn Header="Q2" Width="1*" Binding="{Binding Model.Q2, Converter={StaticResource BoolToStringConverter}}" />
                <DataGridTextColumn Header="Q3" Width="1*" Binding="{Binding Model.Q3, Converter={StaticResource BoolToStringConverter}}" />
                <DataGridTextColumn Header="Q4" Width="1*" Binding="{Binding Model.Q4, Converter={StaticResource BoolToStringConverter}}" />
                <DataGridTextColumn Header="Q5" Width="1*" Binding="{Binding Model.Q5, Converter={StaticResource BoolToStringConverter}}" />
                <DataGridTextColumn Header="Změněno" Width="1*" Binding="{Binding Model.Modified}" />
                <DataGridTextColumn Header="Vytvořeno" Width="*" Binding="{Binding Model.Created}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
