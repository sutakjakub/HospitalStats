<UserControl x:Class="HS.Wpf.ARO.Views.OperationRoomViews.MainOrView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HS.Wpf.ARO.Views.OperationRoomViews"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1200">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HS.Wpf.Shared;component/Theme/Generics.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding LoadedCommand}" />
    </dxmvvm:Interaction.Behaviors>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Button Content="Přidat" Style="{StaticResource ResourceKey=actionBarButton}"/>
            <Button Content="Editovat" Style="{StaticResource ResourceKey=actionBarButton}"/>
            <Button Content="Smazat" Style="{StaticResource ResourceKey=actionBarButton}"/>
            <Button Content="Načíst data" Margin="40 0 0 0" 
                    Style="{StaticResource ResourceKey=actionBarButton}"
                    Command="{Binding LoadDataCommand}" />
        </StackPanel>

        <local:CollectionActionsView DataContext="{Binding CollectionActions}" SelectedAction="{Binding SelectedAction}" Grid.Row="1" Grid.Column="0" Margin="5 5">
            
        </local:CollectionActionsView>

        <local:DetailOrActionView Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>
